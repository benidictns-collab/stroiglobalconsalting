# ─── СГК — Стройглобалконсалтинг ───
# Скопируйте этот файл в .env и заполните значения

# Node environment
NODE_ENV="production"

# Server port (APP Platform требует 3000)
PORT=3000

# Server hostname (обязательно 0.0.0.0 для контейнеров)
HOSTNAME="0.0.0.0"

# Next.js
NEXT_TELEMETRY_DISABLED=1
